allprojects {
	ext.vcs ='svn'
}

apply(plugin: 'idea')

configure(this) {
	apply plugin : 'java'

	sourceSets {
        main {
            java {
                srcDir 'src'
            }
        }
    }
	
	
}